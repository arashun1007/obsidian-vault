# CLAUDE_HANDOFF.md — さかな図鑑 引き継ぎ

最終更新: 2026-07-03 / ブランチ: `claude/flutter-fish-species-d646qf`

## 目標

**「ぼうずコンニャクの市場魚介類図鑑」を超える** 日本の魚介図鑑アプリ。
- 方針: 魚種はできるだけ多く。データは事実ベースで正確に。
- 画像: 現時点では生成しない。後日 **image2** で作成し差し込む。

## 現在の状態（MVP 完成・動作確認済み）

Flutter アプリ `app/` を **ゼロから新規構築**した。
（注: このリポジトリは元 Obsidian 保管庫で、過去の Flutter コードや
以前の AGENTS.md / CLAUDE_HANDOFF.md は存在しなかった。今回新規作成。）

### できていること
- **201 魚種 / 10 カテゴリ**を収録（`app/assets/data/fish.json`）。
  和名・かな・学名・科・旬(月)・最大サイズ・分布・生息域・味わい・料理法・
  別名/地方名・解説を各種に保持。
- **3軸の星評価**（食味 / 価格帯 / 希少度、ハーフスター対応）と
  **味わいレーダーチャート**（脂・旨味・食感・濃厚・香りの5軸）を全種に付与。
  評価は `tool/gen_fish.py` のカテゴリ基準＋主要種の個別調整＋決定的ジッターで生成。
- ホーム画面: グリッド表示、**全文検索**（和名/別名/学名/科）、
  **カテゴリ絞り込み**、**今月の旬フィルタ**、**お気に入りフィルタ**、
  **並び替え**（おすすめ/食味の高い順/価格の安い順/五十音）。カードに食味の星を表示。
- 詳細画面: ヒーロー、カテゴリ、**12ヶ月の旬バー**（当月ハイライト）、
  **評価**（星3軸）、**味わいプロファイル**（レーダー）、解説、基本情報、別名、料理法。
- お気に入り（`shared_preferences`、Web/モバイル/デスクトップ対応・永続化）。
- **プレースホルダ画像**をカテゴリ色＋和名で自動生成（image2 差し替えフック付き）。
- **日本語フォントを必要文字だけサブセット同梱**（各 ~310KB）→ オフライン全環境で表示可。
- `flutter analyze` 警告ゼロ、`flutter test` 12 件パス、`flutter build web` 成功。
  ヘッドレスブラウザで実描画・カード表示・詳細遷移まで確認済み。

### カテゴリ別の収録数
青魚20 / 白身魚37 / 大型回遊魚18 / 根魚14 / 底魚・深海魚19 / 川魚・淡水魚20 /
貝類19 / イカ・タコ14 / エビ・カニ22 / その他・特種18。

## 次にやるべきこと（おすすめ順）

1. **魚種の大幅増量**（最優先・ゴールの本丸）。
   `app/tool/gen_fish.py` の `DATA` に追記 → `gen_fish.py` 実行 →
   `get_font.py` 実行（新漢字対応）。ぼうずコンニャク超えには数百〜が目標。
   候補: カレイ/ハゼ/ベラ類、南方魚、外国産、加工品由来種 など。
2. **image2 で画像生成** → `app/assets/images/` に配置、`fish.json` の
   `imageAsset` を埋める（コード変更不要）。
3. 機能拡張の候補: 五十音/科/旬でのソート・索引、栄養成分、
   産地マップ、毒/注意フラグの明示、詳細のお気に入り一覧タブ、
   データの外部化（サーバ or CSV インポート）。

## 引き継ぎ時の注意
- 作業は必ず `app/` 配下で。ビルド手順・データ増量手順は **AGENTS.md** を参照。
- データはジェネレータ経由で編集（`fish.json` を直接手で書き換えない）。
- 開発ブランチは `claude/flutter-fish-species-d646qf`。
